---
id: installation
slug: /
---

import { Banner, Card } from '../components'
import DockerIcon from '../../static/img/docker-logo.svg'
import KubernetesIcon from '../../static/img/kubernetes-logo.svg'
import OpenshiftIcon from '../../static/img/openshift-logo.svg'
import ExternalLinkIcon from '../../static/img/external-link.svg'

<h3>Step 1. Run Drill4J</h3>
<p>There are 3 ways to deploy Drill4J Admin:</p>

<div class="flex justify-center items-center flex-wrap gap-x-4">
    <button class="button-ghost">
        <DockerIcon/>
        With Docker
    </button>
    <button class="button-ghost">
        <KubernetesIcon/>
        With Kubernetes
    </button>
    <button class="button-ghost">
        <OpenshiftIcon/>
        With Openshift
    </button>
</div>

<h3>Step 2. Deploy your application</h3>
<p>Please choose the agent relevant to your application:</p>

<div class="flex flex-wrap gap-x-5">
    <Card iconSrc={'img/java-logo.svg'}>Java Agent</Card>
    <Card iconSrc={'img/javascript-logo.svg'}>JS Agent</Card>
</div>

<p>To use Drill4J with auto tests, follow this quide (optional):</p>

<Card iconSrc={'img/auto-test-agent-logo.svg'}>Auto test Agent</Card>

<p>For manual testing you need to instal Drill4J Chrome Browser Extension using the following instruction (optional):</p>

<Card iconSrc={'img/browser-extension-logo.svg'}>Chrome Browser Extension</Card>

<h3>Step 3. Register Agent</h3>

<p>
    The last thing you need to do is to register an Agent. Follow this step-by-step
    <a>&nbsp;Guide <ExternalLinkIcon/></a> to comlplete the registration.
</p>
